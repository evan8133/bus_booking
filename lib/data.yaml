drivers:
  {driverId}:
    name:
    licenseNumber:
    contactInfo:

buses:
  {busId}:
    registrationNumber:
    model:
    capacity:
    driverId:

routes:
  {routeId}:
    name:
    start:
    end:
    stops: Array of Geolocation objects

schedules:
  {scheduleId}:
    routeId:
    driverId:
    busId:
    date: (formatted as "YYYY-MM-DD")

bookings:
  {bookingId}:
    userId:
    scheduleId:
    date: (formatted as "YYYY-MM-DD")
    numberOfPersons:
